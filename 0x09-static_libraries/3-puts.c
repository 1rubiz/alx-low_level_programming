#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _puts - prints the strings along with a newline
 * without the null value '\0'.
 * @str: parameter
 *
 * Return: void.
 */

void _puts(char *str)
{
	puts(str);
}
