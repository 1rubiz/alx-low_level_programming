#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _atoi - converts strings to integers
 * @s: parameter.
 *
 * Return: the integers
 */

int _atoi(char *s)
{
	int x = atoi(s);
	return (x);
}
