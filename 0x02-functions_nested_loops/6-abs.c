#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "main.h"
/**
 * _abs- prints the absolute of the argument
 * @int: parameter
 *
 * Return: 0 if successful
 */
int _abs(int);

int _abs(int x)
{
	int abs(int x);
	return (x);
}
