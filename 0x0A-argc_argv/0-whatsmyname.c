#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the name of the function.
 * @argc: Argument counter.
 * @argv: Argument vector.
 *
 * Return: 0 if successful.
 */

int main(char *argv[]) {
	printf("%s\n", argv[0]);
	return (0);
}
